apiVersion: v1
kind: Service
metadata:
  name: web-app-administrasi-service
  labels:
    type: web-app-administrasi-service
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    name: http
    targetPort: http
  selector:
    app: web-app-administrasi
